# konbini Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** cc-sdd にインスパイアされた AI 自律開発フレームワーク "konbini" を npm パッケージとして構築する

**Architecture:** TypeScript CLI がテンプレートファイル（agent定義, SDD commands, rules）をユーザーPJにコピーする。CLIは薄く、コアバリューはテンプレート（特に orchestrator.md）にある。

**Tech Stack:** TypeScript, Node.js, npm (配布), vitest (テスト)

**Spec:** `docs/superpowers/specs/2026-03-13-cc-sdd-ao-design.md`

---

## File Structure

```
konbini/
├── src/
│   ├── cli.ts                          # CLI エントリポイント
│   ├── commands/
│   │   ├── init.ts                     # init コマンド
│   │   ├── update.ts                   # update コマンド
│   │   ├── memory-simplify.ts          # memory simplify コマンド
│   │   └── config-show.ts              # config show コマンド
│   ├── generators/
│   │   ├── template-copier.ts          # テンプレートコピー + プリセット適用
│   │   └── preset-resolver.ts          # プリセット → ao.yaml 値の解決
│   ├── checks/
│   │   ├── tool-checker.ts             # gh, git チェック
│   │   ├── plugin-checker.ts           # Claude Code プラグインチェック
│   │   └── hooks-checker.ts            # pre-commit hooks チェック
│   └── utils/
│       ├── yaml.ts                     # ao.yaml 読み書き
│       ├── paths.ts                    # パス解決
│       └── logger.ts                   # 出力フォーマット
├── templates/
│   ├── agents/
│   │   ├── orchestrator.md             # AOメインループ（最重要）
│   │   ├── implementer.md              # TDD実装ワーカー
│   │   └── reviewer.md                 # マルチ視点レビュー
│   ├── commands/
│   │   ├── spec-init.md
│   │   ├── spec-requirements.md
│   │   ├── spec-design.md
│   │   ├── spec-tasks.md
│   │   ├── spec-impl.md
│   │   ├── spec-status.md
│   │   ├── validate-design.md
│   │   ├── validate-gap.md
│   │   ├── validate-impl.md
│   │   ├── steering.md
│   │   ├── steering-custom.md
│   │   └── ao-run.md                   # AO 自律実行起動コマンド
│   ├── rules/
│   │   ├── ears-format.md
│   │   ├── design-principles.md
│   │   ├── tasks-generation.md
│   │   └── tasks-parallel-analysis.md
│   ├── config/
│   │   └── ao.yaml.template
│   ├── steering/
│   │   ├── product.md.template
│   │   ├── tech.md.template
│   │   └── structure.md.template
│   ├── memory/
│   │   └── index.md.template
│   └── spec-templates/
│       ├── requirements-init.md
│       ├── requirements.md
│       ├── design.md
│       ├── tasks.md
│       └── research.md
├── test/
│   ├── commands/
│   │   ├── init.test.ts
│   │   ├── update.test.ts
│   │   └── config-show.test.ts
│   ├── generators/
│   │   ├── template-copier.test.ts
│   │   └── preset-resolver.test.ts
│   └── checks/
│       ├── tool-checker.test.ts
│       └── hooks-checker.test.ts
├── package.json
├── tsconfig.json
└── README.md
```

---

## Chunk 1: Project Scaffolding + CLI Foundation

### Task 1: リポジトリ初期化

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `.gitignore`
- Create: `src/cli.ts`

- [ ] **Step 1: GitHub リポジトリ作成**

```bash
cd /Users/gene/projects
gh repo create kaze-jp/konbini --public --clone
cd konbini
```

- [ ] **Step 2: package.json 作成**

```json
{
  "name": "konbini",
  "version": "0.1.0",
  "description": "AI autonomous development framework inspired by cc-sdd. Spec-Driven Development with Agent Orchestrator.",
  "keywords": [
    "claude-code",
    "sdd",
    "spec-driven-development",
    "agent-orchestrator",
    "autonomous-development",
    "tdd",
    "ai-development"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kaze-jp/konbini.git"
  },
  "type": "module",
  "bin": {
    "konbini": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "postbuild": "node scripts/add-shebang.mjs",
    "test": "vitest run",
    "test:watch": "vitest -w",
    "prepare": "npm run build"
  },
  "files": [
    "dist",
    "templates"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 3: tsconfig.json 作成**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "declaration": true,
    "skipLibCheck": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist", "test"]
}
```

- [ ] **Step 4: .gitignore 作成**

```
node_modules/
dist/
*.tgz
```

- [ ] **Step 5: 依存関係インストール**

```bash
npm install
```

- [ ] **Step 6: add-shebang スクリプト作成**

```javascript
// scripts/add-shebang.mjs
import { readFileSync, writeFileSync } from 'fs';
const file = 'dist/cli.js';
const content = readFileSync(file, 'utf8');
if (!content.startsWith('#!')) {
  writeFileSync(file, '#!/usr/bin/env node\n' + content);
}
```

- [ ] **Step 7: Commit**

```bash
git add package.json tsconfig.json .gitignore scripts/
git commit -m "chore: initialize konbini project"
```

---

### Task 2: CLI エントリポイント + サブコマンドルーティング

**Files:**
- Create: `src/cli.ts`
- Create: `src/utils/logger.ts`
- Test: `test/cli.test.ts`

- [ ] **Step 1: logger ユーティリティ作成**

```typescript
// src/utils/logger.ts
export const log = {
  info: (msg: string) => console.log(`  ${msg}`),
  success: (msg: string) => console.log(`  ✓ ${msg}`),
  warn: (msg: string) => console.log(`  ⚠ ${msg}`),
  error: (msg: string) => console.error(`  ✗ ${msg}`),
  header: (msg: string) => console.log(`\n  ${msg}\n${'  ' + '─'.repeat(40)}`),
};
```

- [ ] **Step 2: CLI failing test 作成**

```typescript
// test/cli.test.ts
import { describe, it, expect } from 'vitest';
import { parseArgs } from '../src/cli.js';

describe('parseArgs', () => {
  it('parses init command', () => {
    expect(parseArgs(['init'])).toEqual({ command: 'init', args: [] });
  });

  it('parses update command', () => {
    expect(parseArgs(['update'])).toEqual({ command: 'update', args: [] });
  });

  it('parses memory simplify command', () => {
    expect(parseArgs(['memory', 'simplify'])).toEqual({ command: 'memory-simplify', args: [] });
  });

  it('parses config show command', () => {
    expect(parseArgs(['config', 'show'])).toEqual({ command: 'config-show', args: [] });
  });

  it('returns help for no args', () => {
    expect(parseArgs([])).toEqual({ command: 'help', args: [] });
  });
});
```

- [ ] **Step 3: テスト実行して失敗確認**

```bash
npx vitest run test/cli.test.ts
```

Expected: FAIL (module not found)

- [ ] **Step 4: CLI 実装**

```typescript
// src/cli.ts
import { log } from './utils/logger.js';

export interface ParsedCommand {
  command: string;
  args: string[];
}

export function parseArgs(args: string[]): ParsedCommand {
  if (args.length === 0) return { command: 'help', args: [] };

  const [first, second, ...rest] = args;

  if (first === 'memory' && second === 'simplify') {
    return { command: 'memory-simplify', args: rest };
  }
  if (first === 'config' && second === 'show') {
    return { command: 'config-show', args: rest };
  }

  return { command: first, args: args.slice(1) };
}

const HELP = `
  konbini — AI autonomous development framework

  Usage:
    npx konbini init              Initialize konbini in current project
    npx konbini update            Update templates (preserves config & memory)
    npx konbini memory simplify   Consolidate learned review patterns
    npx konbini config show       Show current configuration

  Inspired by cc-sdd (https://github.com/gotalab/cc-sdd)
`;

async function main() {
  const { command, args } = parseArgs(process.argv.slice(2));

  switch (command) {
    case 'init': {
      const { runInit } = await import('./commands/init.js');
      await runInit(args);
      break;
    }
    case 'update': {
      const { runUpdate } = await import('./commands/update.js');
      await runUpdate(args);
      break;
    }
    case 'memory-simplify': {
      const { runMemorySimplify } = await import('./commands/memory-simplify.js');
      await runMemorySimplify(args);
      break;
    }
    case 'config-show': {
      const { runConfigShow } = await import('./commands/config-show.js');
      await runConfigShow(args);
      break;
    }
    default:
      console.log(HELP);
  }
}

main().catch((err) => {
  log.error(err.message);
  process.exit(1);
});
```

- [ ] **Step 5: テスト実行して成功確認**

```bash
npx vitest run test/cli.test.ts
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/cli.ts src/utils/logger.ts test/cli.test.ts
git commit -m "feat: CLI entry point with subcommand routing"
```

---

### Task 3: パスユーティリティ + YAML ユーティリティ

**Files:**
- Create: `src/utils/paths.ts`
- Create: `src/utils/yaml.ts`
- Test: `test/utils/paths.test.ts`
- Test: `test/utils/yaml.test.ts`

- [ ] **Step 1: paths failing test**

```typescript
// test/utils/paths.test.ts
import { describe, it, expect } from 'vitest';
import { getTemplatePath, getTargetPaths } from '../../src/utils/paths.js';
import path from 'path';

describe('getTemplatePath', () => {
  it('resolves to templates directory relative to package', () => {
    const result = getTemplatePath('agents/orchestrator.md');
    expect(result).toContain('templates/agents/orchestrator.md');
  });
});

describe('getTargetPaths', () => {
  it('returns correct target directories for a project root', () => {
    const root = '/test/project';
    const paths = getTargetPaths(root);
    expect(paths.aoConfig).toBe('/test/project/.ao/ao.yaml');
    expect(paths.agents).toBe('/test/project/.claude/agents');
    expect(paths.commands).toBe('/test/project/.claude/commands/kiro');
    expect(paths.memory).toBe('/test/project/.ao/memory');
    expect(paths.steering).toBe('/test/project/.ao/steering');
    expect(paths.rules).toBe('/test/project/.kiro/settings/rules');
    expect(paths.specTemplates).toBe('/test/project/.kiro/settings/templates/specs');
  });
});
```

- [ ] **Step 2: paths 実装**

```typescript
// src/utils/paths.ts
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));

export function getTemplatePath(relativePath: string): string {
  return path.resolve(__dirname, '..', '..', 'templates', relativePath);
}

export interface TargetPaths {
  root: string;
  aoConfig: string;
  agents: string;
  commands: string;
  memory: string;
  memoryReviewPatterns: string;
  memoryProjectContext: string;
  steering: string;
  rules: string;
  specTemplates: string;
}

export function getTargetPaths(projectRoot: string): TargetPaths {
  return {
    root: projectRoot,
    aoConfig: path.join(projectRoot, '.ao', 'ao.yaml'),
    agents: path.join(projectRoot, '.claude', 'agents'),
    commands: path.join(projectRoot, '.claude', 'commands', 'kiro'),
    memory: path.join(projectRoot, '.ao', 'memory'),
    memoryReviewPatterns: path.join(projectRoot, '.ao', 'memory', 'review-patterns'),
    memoryProjectContext: path.join(projectRoot, '.ao', 'memory', 'project-context'),
    steering: path.join(projectRoot, '.ao', 'steering'),
    rules: path.join(projectRoot, '.kiro', 'settings', 'rules'),
    specTemplates: path.join(projectRoot, '.kiro', 'settings', 'templates', 'specs'),
  };
}
```

- [ ] **Step 3: yaml failing test**

```typescript
// test/utils/yaml.test.ts
import { describe, it, expect } from 'vitest';
import { parseTopLevelYaml } from '../../src/utils/yaml.js';

describe('parseTopLevelYaml', () => {
  it('parses top-level keys only', () => {
    const input = 'preset: solo\nschema_version: 1\nautonomy:\n  upstream: manual';
    const result = parseTopLevelYaml(input);
    expect(result.preset).toBe('solo');
    expect(result.schema_version).toBe(1);
    // nested keys are ignored
    expect(result.upstream).toBeUndefined();
  });

  it('ignores comments', () => {
    const result = parseTopLevelYaml('# comment\npreset: team');
    expect(result.preset).toBe('team');
  });
});
```

- [ ] **Step 4: yaml 実装（最小限の自前パーサー、外部依存なし）**

```typescript
// src/utils/yaml.ts
// ao.yaml は構造が固定なのでテンプレート置換方式で生成する。
// parseYaml はトップレベルキーの読み取り専用（schema_version等）。
// 完全なYAMLパースが必要になった場合は yaml パッケージを追加する。
// 注意: ネストされたキーは読み取れない。フルパースが必要なら要リファクタ。

export function parseTopLevelYaml(content: string): Record<string, unknown> {
  const result: Record<string, unknown> = {};
  for (const line of content.split('\n')) {
    const trimmed = line.trim();
    if (!trimmed || trimmed.startsWith('#')) continue;
    // トップレベルのみ（インデントなし）
    if (line.startsWith(' ') || line.startsWith('\t')) continue;
    const match = trimmed.match(/^(\w+):\s*(.+)$/);
    if (match) {
      const [, key, value] = match;
      if (value === 'true') result[key] = true;
      else if (value === 'false') result[key] = false;
      else if (/^\d+$/.test(value)) result[key] = parseInt(value, 10);
      else result[key] = value;
    }
  }
  return result;
}
```

- [ ] **Step 5: テスト実行**

```bash
npx vitest run test/utils/
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/utils/ test/utils/
git commit -m "feat: path resolution and yaml utilities"
```

---

## Chunk 2: Prerequisite Checkers + Template Copier

### Task 4: ツール・プラグイン・Hooks チェッカー

**Files:**
- Create: `src/checks/tool-checker.ts`
- Create: `src/checks/plugin-checker.ts`
- Create: `src/checks/hooks-checker.ts`
- Test: `test/checks/tool-checker.test.ts`
- Test: `test/checks/hooks-checker.test.ts`

- [ ] **Step 1: tool-checker failing test**

```typescript
// test/checks/tool-checker.test.ts
import { describe, it, expect, vi } from 'vitest';
import { checkTool } from '../../src/checks/tool-checker.js';

describe('checkTool', () => {
  it('returns true for available tools', async () => {
    // git should be available in test environment
    const result = await checkTool('git');
    expect(result.available).toBe(true);
  });

  it('returns false for unavailable tools', async () => {
    const result = await checkTool('nonexistent-tool-xyz');
    expect(result.available).toBe(false);
  });
});
```

- [ ] **Step 2: tool-checker 実装**

```typescript
// src/checks/tool-checker.ts
import { execSync } from 'child_process';

export interface ToolCheckResult {
  name: string;
  available: boolean;
  version?: string;
}

export async function checkTool(name: string): Promise<ToolCheckResult> {
  try {
    const version = execSync(`${name} --version`, { encoding: 'utf-8', stdio: ['pipe', 'pipe', 'pipe'] }).trim();
    return { name, available: true, version };
  } catch {
    return { name, available: false };
  }
}

export async function checkAllTools(): Promise<ToolCheckResult[]> {
  return Promise.all([checkTool('git'), checkTool('gh')]);
}
```

- [ ] **Step 3: plugin-checker 実装**

```typescript
// src/checks/plugin-checker.ts
import { execSync } from 'child_process';
import { log } from '../utils/logger.js';

const REQUIRED_PLUGINS = [
  { name: 'feature-dev', purpose: '実装ガイド' },
  { name: 'code-review', purpose: 'PRレビュー' },
  { name: 'superpowers', purpose: 'simplify, brainstorming, TDD等' },
] as const;

export interface PluginCheckResult {
  name: string;
  purpose: string;
  available: boolean;
}

export function checkPlugins(): PluginCheckResult[] {
  // Claude Code のプラグイン検出は環境依存。
  // init 時にユーザーに確認する形がベスト。
  return REQUIRED_PLUGINS.map((p) => ({
    ...p,
    available: false, // init 時にユーザーに確認
  }));
}

export function printPluginInstructions(missing: PluginCheckResult[]) {
  if (missing.length === 0) return;
  log.header('Required Claude Code Plugins');
  log.warn('以下のプラグインをインストールしてください:');
  for (const p of missing) {
    log.info(`  ${p.name} — ${p.purpose}`);
  }
  log.info('');
  log.info('Claude Code 内で /install-plugin <name> を実行してください。');
}
```

- [ ] **Step 4: hooks-checker failing test**

```typescript
// test/checks/hooks-checker.test.ts
import { describe, it, expect } from 'vitest';
import { checkPreCommitHooks } from '../../src/checks/hooks-checker.js';
import path from 'path';
import fs from 'fs';
import os from 'os';

describe('checkPreCommitHooks', () => {
  it('returns false when no hooks directory exists', () => {
    const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'konbini-test-'));
    fs.mkdirSync(path.join(tmpDir, '.git'), { recursive: true });
    const result = checkPreCommitHooks(tmpDir);
    expect(result.hasHooks).toBe(false);
    fs.rmSync(tmpDir, { recursive: true });
  });
});
```

- [ ] **Step 5: hooks-checker 実装**

```typescript
// src/checks/hooks-checker.ts
import fs from 'fs';
import path from 'path';
import { log } from '../utils/logger.js';

export interface HooksCheckResult {
  hasHooks: boolean;
  hasPreCommit: boolean;
}

export function checkPreCommitHooks(projectRoot: string): HooksCheckResult {
  const huskyDir = path.join(projectRoot, '.husky');
  const gitHooksDir = path.join(projectRoot, '.git', 'hooks', 'pre-commit');
  const lintStagedConfig = ['lint-staged.config.js', 'lint-staged.config.mjs', '.lintstagedrc']
    .some((f) => fs.existsSync(path.join(projectRoot, f)));

  const hasHusky = fs.existsSync(huskyDir);
  const hasGitHook = fs.existsSync(gitHooksDir);

  return {
    hasHooks: hasHusky || hasGitHook || lintStagedConfig,
    hasPreCommit: hasGitHook || hasHusky,
  };
}

export function printHooksRecommendation() {
  log.header('Pre-commit Hooks (推奨)');
  log.warn('pre-commit hooks が検出されませんでした。以下の設定を推奨します:');
  log.info('  • lint-staged — 変更ファイルのみ lint');
  log.info('  • type-check — 型チェック');
  log.info('  • test — 関連テストの実行');
  log.info('');
  log.warn('git commit --no-verify は使用しないことを強く推奨します。');
}
```

- [ ] **Step 6: テスト実行**

```bash
npx vitest run test/checks/
```

Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add src/checks/ test/checks/
git commit -m "feat: prerequisite checkers (tools, plugins, hooks)"
```

---

**注意**: Task 5 の template-copier テストは Task 7-13 のテンプレートファイルに依存する。Task 5 では最小限のダミーテンプレートを `test/fixtures/` に作成してテストし、実テンプレートとの結合テストは Task 17 で行う。

### Task 5: プリセットリゾルバー + テンプレートコピー

**Files:**
- Create: `src/generators/preset-resolver.ts`
- Create: `src/generators/template-copier.ts`
- Test: `test/generators/preset-resolver.test.ts`
- Test: `test/generators/template-copier.test.ts`

- [ ] **Step 1: preset-resolver failing test**

```typescript
// test/generators/preset-resolver.test.ts
import { describe, it, expect } from 'vitest';
import { resolvePreset } from '../../src/generators/preset-resolver.js';

describe('resolvePreset', () => {
  it('resolves solo preset', () => {
    const config = resolvePreset('solo');
    expect(config.downstream).toBe('approve-only');
    expect(config.R8_actor).toBe('human');
  });

  it('resolves solo-full-auto preset', () => {
    const config = resolvePreset('solo-full-auto');
    expect(config.downstream).toBe('full-auto');
    expect(config.R8_actor).toBe('skip');
  });

  it('resolves team preset', () => {
    const config = resolvePreset('team');
    expect(config.downstream).toBe('review-and-approve');
    expect(config.R8_actor).toBe('human');
  });
});
```

- [ ] **Step 2: preset-resolver 実装**

```typescript
// src/generators/preset-resolver.ts
export type PresetName = 'solo' | 'solo-full-auto' | 'team';

export interface PresetConfig {
  downstream: string;
  R8_actor: string;
  auto_merge: boolean;
}

const PRESETS: Record<PresetName, PresetConfig> = {
  solo: {
    downstream: 'approve-only',
    R8_actor: 'human',
    auto_merge: true,
  },
  'solo-full-auto': {
    downstream: 'full-auto',
    R8_actor: 'skip',
    auto_merge: true,
  },
  team: {
    downstream: 'review-and-approve',
    R8_actor: 'human',
    auto_merge: true,
  },
};

export function resolvePreset(name: string): PresetConfig {
  const preset = PRESETS[name as PresetName];
  if (!preset) throw new Error(`Unknown preset: ${name}. Valid: ${Object.keys(PRESETS).join(', ')}`);
  return preset;
}

export function getPresetNames(): PresetName[] {
  return Object.keys(PRESETS) as PresetName[];
}
```

- [ ] **Step 3: template-copier failing test（テスト用フィクスチャ使用）**

```typescript
// test/generators/template-copier.test.ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { copyTemplates } from '../../src/generators/template-copier.js';
import fs from 'fs';
import path from 'path';
import os from 'os';

// テスト用ダミーテンプレートを事前作成
// test/fixtures/templates/ に最小限のテンプレートを配置
// getTemplatePath をモックしてフィクスチャを指すようにする

describe('copyTemplates', () => {
  let tmpDir: string;
  const fixtureDir = path.resolve(__dirname, '../fixtures/templates');

  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'konbini-test-'));
    // getTemplatePath をフィクスチャにリダイレクト
    vi.mock('../../src/utils/paths.js', async (importOriginal) => {
      const actual = await importOriginal<typeof import('../../src/utils/paths.js')>();
      return {
        ...actual,
        getTemplatePath: (rel: string) => path.join(fixtureDir, rel),
      };
    });
  });

  afterEach(() => {
    fs.rmSync(tmpDir, { recursive: true });
    vi.restoreAllMocks();
  });

  it('creates .ao directory structure', async () => {
    await copyTemplates(tmpDir, 'solo');
    expect(fs.existsSync(path.join(tmpDir, '.ao', 'ao.yaml'))).toBe(true);
  });

  it('applies preset to ao.yaml', async () => {
    await copyTemplates(tmpDir, 'solo-full-auto');
    const content = fs.readFileSync(path.join(tmpDir, '.ao', 'ao.yaml'), 'utf-8');
    expect(content).toContain('preset: solo-full-auto');
    expect(content).toContain('downstream: full-auto');
  });

  it('does not overwrite existing ao.yaml', async () => {
    const aoDir = path.join(tmpDir, '.ao');
    fs.mkdirSync(aoDir, { recursive: true });
    fs.writeFileSync(path.join(aoDir, 'ao.yaml'), 'existing: true');
    await copyTemplates(tmpDir, 'solo', { overwrite: false });
    const content = fs.readFileSync(path.join(aoDir, 'ao.yaml'), 'utf-8');
    expect(content).toBe('existing: true');
  });
});
```

テスト用フィクスチャも作成:

```
test/fixtures/templates/
├── agents/orchestrator.md      # "# Orchestrator" (最小限)
├── agents/implementer.md       # "# Implementer"
├── agents/reviewer.md          # "# Reviewer"
├── config/ao.yaml.template     # "preset: {{PRESET}}\ndownstream: {{DOWNSTREAM}}"
└── memory/index.md.template    # "# Memory Index"
```
```

- [ ] **Step 4: template-copier 実装**

```typescript
// src/generators/template-copier.ts
import fs from 'fs';
import path from 'path';
import { getTemplatePath, getTargetPaths } from '../utils/paths.js';
import { resolvePreset } from './preset-resolver.js';
import { log } from '../utils/logger.js';

interface CopyOptions {
  overwrite?: boolean;
}

function copyFileIfNotExists(src: string, dest: string, overwrite: boolean): boolean {
  if (!overwrite && fs.existsSync(dest)) return false;
  fs.mkdirSync(path.dirname(dest), { recursive: true });
  fs.copyFileSync(src, dest);
  return true;
}

function copyDir(srcDir: string, destDir: string, overwrite: boolean) {
  if (!fs.existsSync(srcDir)) return;
  fs.mkdirSync(destDir, { recursive: true });
  for (const file of fs.readdirSync(srcDir)) {
    const srcPath = path.join(srcDir, file);
    const destPath = path.join(destDir, file);
    if (fs.statSync(srcPath).isDirectory()) {
      copyDir(srcPath, destPath, overwrite);
    } else {
      copyFileIfNotExists(srcPath, destPath, overwrite);
    }
  }
}

export async function copyTemplates(
  projectRoot: string,
  presetName: string,
  options: CopyOptions = {}
) {
  const { overwrite = true } = options;
  const paths = getTargetPaths(projectRoot);
  const preset = resolvePreset(presetName);

  // Agents
  copyDir(getTemplatePath('agents'), paths.agents, overwrite);

  // Commands
  copyDir(getTemplatePath('commands'), paths.commands, overwrite);

  // Rules
  copyDir(getTemplatePath('rules'), paths.rules, overwrite);

  // Spec templates
  copyDir(getTemplatePath('spec-templates'), paths.specTemplates, overwrite);

  // Steering templates
  for (const file of ['product.md.template', 'tech.md.template', 'structure.md.template']) {
    const src = getTemplatePath(`steering/${file}`);
    const dest = path.join(paths.steering, file.replace('.template', ''));
    if (fs.existsSync(src)) {
      copyFileIfNotExists(src, dest, false); // steering は上書きしない
    }
  }

  // Memory structure
  fs.mkdirSync(paths.memoryReviewPatterns, { recursive: true });
  fs.mkdirSync(paths.memoryProjectContext, { recursive: true });
  const memoryIndex = getTemplatePath('memory/index.md.template');
  if (fs.existsSync(memoryIndex)) {
    copyFileIfNotExists(memoryIndex, path.join(paths.memory, 'index.md'), false);
  }

  // ao.yaml (テンプレートからプリセット値を適用して生成)
  const aoTemplate = fs.readFileSync(getTemplatePath('config/ao.yaml.template'), 'utf-8');
  const aoContent = aoTemplate
    .replace('{{PRESET}}', presetName)
    .replace('{{DOWNSTREAM}}', preset.downstream)
    .replace('{{R8_ACTOR}}', preset.R8_actor)
    .replace('{{AUTO_MERGE}}', String(preset.auto_merge));

  const aoPath = paths.aoConfig;
  if (overwrite || !fs.existsSync(aoPath)) {
    fs.mkdirSync(path.dirname(aoPath), { recursive: true });
    fs.writeFileSync(aoPath, aoContent);
  }

  log.success('テンプレートを展開しました');
}
```

- [ ] **Step 5: テスト実行**

```bash
npx vitest run test/generators/
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/generators/ test/generators/
git commit -m "feat: preset resolver and template copier"
```

---

### Task 6: init コマンド統合

**Files:**
- Create: `src/commands/init.ts`
- Test: `test/commands/init.test.ts`

- [ ] **Step 1: init failing test**

```typescript
// test/commands/init.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import fs from 'fs';
import path from 'path';
import os from 'os';

describe('init command', () => {
  let tmpDir: string;

  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'konbini-init-'));
    fs.mkdirSync(path.join(tmpDir, '.git'), { recursive: true });
  });

  afterEach(() => {
    fs.rmSync(tmpDir, { recursive: true });
  });

  it('generates expected directory structure', async () => {
    const { initProject } = await import('../../src/commands/init.js');
    await initProject(tmpDir, 'solo');
    expect(fs.existsSync(path.join(tmpDir, '.ao', 'ao.yaml'))).toBe(true);
    expect(fs.existsSync(path.join(tmpDir, '.claude', 'agents', 'orchestrator.md'))).toBe(true);
    expect(fs.existsSync(path.join(tmpDir, '.claude', 'commands', 'kiro'))).toBe(true);
    expect(fs.existsSync(path.join(tmpDir, '.kiro', 'settings', 'rules'))).toBe(true);
  });
});
```

- [ ] **Step 2: init 実装**

```typescript
// src/commands/init.ts
import { checkAllTools } from '../checks/tool-checker.js';
import { checkPlugins, printPluginInstructions } from '../checks/plugin-checker.js';
import { checkPreCommitHooks, printHooksRecommendation } from '../checks/hooks-checker.js';
import { copyTemplates } from '../generators/template-copier.js';
import { getPresetNames } from '../generators/preset-resolver.js';
import { log } from '../utils/logger.js';

export async function initProject(projectRoot: string, preset: string) {
  log.header('konbini init');

  // 1. ツールチェック
  const tools = await checkAllTools();
  for (const tool of tools) {
    if (tool.available) {
      log.success(`${tool.name} ${tool.version ?? ''}`);
    } else {
      log.error(`${tool.name} が見つかりません`);
    }
  }

  // 2. プラグイン案内
  const plugins = checkPlugins();
  printPluginInstructions(plugins);

  // 3. Hooks チェック
  const hooks = checkPreCommitHooks(projectRoot);
  if (!hooks.hasHooks) {
    printHooksRecommendation();
  }

  // 4. テンプレート展開
  await copyTemplates(projectRoot, preset);

  log.success(`konbini initialized with preset: ${preset}`);
  log.info('');
  log.info('Next: Claude Code で /kiro:spec-init <feature> を実行してください');
}

export async function runInit(args: string[]) {
  let preset = args[0];
  const validPresets = getPresetNames();

  if (!preset) {
    // インタラクティブ選択
    const readline = await import('readline');
    const rl = readline.createInterface({ input: process.stdin, output: process.stdout });
    const question = (q: string) => new Promise<string>((resolve) => rl.question(q, resolve));

    log.header('Preset Selection');
    log.info('  1) solo           — 上流承認 + 下流は approve-only（推奨）');
    log.info('  2) solo-full-auto — 上流承認後は完全自律');
    log.info('  3) team           — 上流承認 + 下流は review-and-approve');
    const answer = await question('\n  Select preset [1]: ');
    rl.close();

    const map: Record<string, string> = { '1': 'solo', '2': 'solo-full-auto', '3': 'team', '': 'solo' };
    preset = map[answer.trim()] ?? 'solo';
  }

  if (!validPresets.includes(preset as any)) {
    log.error(`Invalid preset: ${preset}. Valid: ${validPresets.join(', ')}`);
    process.exit(1);
  }
  await initProject(process.cwd(), preset);
}
```

- [ ] **Step 3: テスト実行**

```bash
npx vitest run test/commands/init.test.ts
```

Expected: PASS (テンプレートファイルが存在すれば)

- [ ] **Step 4: Commit**

```bash
git add src/commands/init.ts test/commands/init.test.ts
git commit -m "feat: init command with tool/plugin/hooks checks"
```

---

## Chunk 3: Core Templates — ao.yaml + Agents

### Task 7: ao.yaml テンプレート

**Files:**
- Create: `templates/config/ao.yaml.template`

- [ ] **Step 1: ao.yaml.template 作成**

ao.yaml.template にはスペックの Configuration セクション全体を `{{PLACEHOLDER}}` 付きで書き出す。スペック `docs/superpowers/specs/2026-03-13-cc-sdd-ao-design.md` の lines 129-310 をテンプレート化する。プリセット値（`{{PRESET}}`, `{{DOWNSTREAM}}`, `{{R8_ACTOR}}`, `{{AUTO_MERGE}}`）を置換可能にする。

- [ ] **Step 2: Commit**

```bash
git add templates/config/
git commit -m "feat: ao.yaml template with preset placeholders"
```

---

### Task 8: Orchestrator エージェント（最重要）

**Files:**
- Create: `templates/agents/orchestrator.md`

orchestrator.md は konbini の頭脳。スペックの Orchestrator Execution Loop セクション全体をエージェント定義として落とし込む。

- [ ] **Step 1: orchestrator.md 作成**

以下を含む:
- AOの役割定義（"I ship, I code" 哲学）
- Phase 1〜R8 の全フロー定義
- 各フェーズで使うスキルの明示的指示
- チェックポイントフォーマット（プログレスバー付き）
- autonomy.downstream の分岐ロジック
- エスカレーション条件と挙動
- GH レビューコメントの書式
- memory 蓄積ルール

参考: headshot-ai の `.claude/agents/sdd.md`（550行）をベースに、konbini の設計に合わせて汎用化する。PJ固有の参照（Replicate, Supabase等）をすべて除去し、ao.yaml から設定値を読む形にする。

- [ ] **Step 2: Commit**

```bash
git add templates/agents/orchestrator.md
git commit -m "feat: orchestrator agent — AO core brain"
```

---

### Task 9: Implementer エージェント

**Files:**
- Create: `templates/agents/implementer.md`

- [ ] **Step 1: implementer.md 作成**

以下を含む:
- TDD 必須（Red → Green → Refactor）
- /feature-dev:feature-dev + /superpowers:test-driven-development の使用指示
- worktree 内での作業スコープ制限
- 品質ゲート（型チェック, lint, test, build）の実行
- TaskUpdate によるタスク完了報告
- .ao/context/ の TaskContextBrief 読み込み指示

参考: headshot-ai の `.claude/agents/implementer.md`（140行）

- [ ] **Step 2: Commit**

```bash
git add templates/agents/implementer.md
git commit -m "feat: implementer agent — TDD worker"
```

---

### Task 10: Reviewer エージェント

**Files:**
- Create: `templates/agents/reviewer.md`

- [ ] **Step 1: reviewer.md 作成**

以下を含む:
- 3モード: Design Review / Task Review / Code Review
- Code Review: 5視点（Spec準拠, 一貫性, セキュリティ, パフォーマンス, ドキュメンテーション）
- specialist 選択ロジック（auto_select に基づく）
- .ao/memory/review-patterns/ からの学習パターン注入
- GH comment フォーマット（🤖 [ao-review/{role}]）
- 判定基準: Critical / High / Warning / Info
- /code-review:code-review + /superpowers:requesting-code-review の使用指示

参考: headshot-ai の `.claude/agents/reviewer.md`（420行）

- [ ] **Step 2: Commit**

```bash
git add templates/agents/reviewer.md
git commit -m "feat: reviewer agent — multi-specialist review"
```

---

## Chunk 4: SDD Commands + Rules

### Task 11: kiro 互換コマンド群

**Files:**
- Create: `templates/commands/spec-init.md`
- Create: `templates/commands/spec-requirements.md`
- Create: `templates/commands/spec-design.md`
- Create: `templates/commands/spec-tasks.md`
- Create: `templates/commands/spec-impl.md`
- Create: `templates/commands/spec-status.md`
- Create: `templates/commands/validate-design.md`
- Create: `templates/commands/validate-gap.md`
- Create: `templates/commands/validate-impl.md`
- Create: `templates/commands/steering.md`
- Create: `templates/commands/steering-custom.md`
- Create: `templates/commands/ao-run.md`

- [ ] **Step 1: headshot-ai の既存コマンドを参考に汎用化**

各コマンドについて headshot-ai の `.claude/commands/kiro/` から読み取り、PJ固有の参照を除去して汎用テンプレートにする。

- [ ] **Step 2: ao-run.md 新規作成**

AO 自律実行を起動するための新コマンド。ユーザーが `/kiro:ao-run <feature>` と実行すると orchestrator が起動し、Phase 1 から自律実行を開始する。

- [ ] **Step 3: Commit**

```bash
git add templates/commands/
git commit -m "feat: kiro-compatible SDD commands (12 commands)"
```

---

### Task 12: ルールファイル

**Files:**
- Create: `templates/rules/ears-format.md`
- Create: `templates/rules/design-principles.md`
- Create: `templates/rules/tasks-generation.md`
- Create: `templates/rules/tasks-parallel-analysis.md`

- [ ] **Step 1: headshot-ai のルールを参考に汎用化**

`.kiro/settings/rules/` の4ファイルから PJ 固有の参照を除去して汎用テンプレートにする。

- [ ] **Step 2: Commit**

```bash
git add templates/rules/
git commit -m "feat: SDD rules (EARS, design principles, task generation)"
```

---

### Task 13: Spec テンプレート + Steering テンプレート + Memory テンプレート

**Files:**
- Create: `templates/spec-templates/requirements-init.md`
- Create: `templates/spec-templates/requirements.md`
- Create: `templates/spec-templates/design.md`
- Create: `templates/spec-templates/tasks.md`
- Create: `templates/spec-templates/research.md`
- Create: `templates/steering/product.md.template`
- Create: `templates/steering/tech.md.template`
- Create: `templates/steering/structure.md.template`
- Create: `templates/memory/index.md.template`

- [ ] **Step 1: spec テンプレート作成**

cc-sdd の spec テンプレート構造を参考に作成。

- [ ] **Step 2: steering テンプレート作成**

プロダクト / テック / 構造の3テンプレート。`{{PLACEHOLDER}}` でユーザーが埋める形。

- [ ] **Step 3: memory index テンプレート作成**

```markdown
# konbini Memory Index

## Review Patterns
(自動蓄積されます)

## Project Context
(自動蓄積されます)
```

- [ ] **Step 4: Commit**

```bash
git add templates/spec-templates/ templates/steering/ templates/memory/
git commit -m "feat: spec, steering, and memory templates"
```

---

### Task 13.5: テンプレートスモークテスト

**Files:**
- Create: `test/templates/smoke.test.ts`

- [ ] **Step 1: スモークテスト作成**

```typescript
// test/templates/smoke.test.ts
import { describe, it, expect } from 'vitest';
import fs from 'fs';
import path from 'path';
import { getTemplatePath } from '../../src/utils/paths.js';

const REQUIRED_TEMPLATES = [
  'agents/orchestrator.md',
  'agents/implementer.md',
  'agents/reviewer.md',
  'config/ao.yaml.template',
  'memory/index.md.template',
];

const REQUIRED_KEYWORDS: Record<string, string[]> = {
  'agents/orchestrator.md': ['Phase 1', 'Phase 1.5', 'R4', 'R5', 'R6', 'R7', 'R8', 'escalation', 'ao.yaml'],
  'agents/implementer.md': ['TDD', 'Red', 'Green', 'Refactor', 'worktree', 'TaskUpdate'],
  'agents/reviewer.md': ['security', 'quality', 'frontend', 'backend', 'memory', 'ao-review'],
  'config/ao.yaml.template': ['{{PRESET}}', '{{DOWNSTREAM}}', 'schema_version'],
};

describe('template files', () => {
  for (const template of REQUIRED_TEMPLATES) {
    it(`${template} exists`, () => {
      const fullPath = getTemplatePath(template);
      expect(fs.existsSync(fullPath)).toBe(true);
    });
  }

  for (const [template, keywords] of Object.entries(REQUIRED_KEYWORDS)) {
    it(`${template} contains required keywords`, () => {
      const content = fs.readFileSync(getTemplatePath(template), 'utf-8');
      for (const keyword of keywords) {
        expect(content).toContain(keyword);
      }
    });
  }
});
```

- [ ] **Step 2: テスト実行（全テンプレートが Task 7-13 で作成済みであること）**

```bash
npx vitest run test/templates/smoke.test.ts
```

Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add test/templates/
git commit -m "test: template smoke tests for required files and keywords"
```

---

## Chunk 5: Remaining CLI Commands + README

### Task 14: update コマンド

**Files:**
- Create: `src/commands/update.ts`
- Test: `test/commands/update.test.ts`

- [ ] **Step 1: update failing test**

```typescript
// test/commands/update.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import fs from 'fs';
import path from 'path';
import os from 'os';
import { updateProject } from '../../src/commands/update.js';

describe('update command', () => {
  let tmpDir: string;

  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'konbini-update-'));
  });

  afterEach(() => {
    fs.rmSync(tmpDir, { recursive: true });
  });

  it('updates agent files', async () => {
    // Create initial structure
    const agentDir = path.join(tmpDir, '.claude', 'agents');
    fs.mkdirSync(agentDir, { recursive: true });
    fs.writeFileSync(path.join(agentDir, 'orchestrator.md'), 'old content');

    await updateProject(tmpDir);
    const content = fs.readFileSync(path.join(agentDir, 'orchestrator.md'), 'utf-8');
    expect(content).not.toBe('old content');
  });

  it('preserves ao.yaml', async () => {
    const aoDir = path.join(tmpDir, '.ao');
    fs.mkdirSync(aoDir, { recursive: true });
    fs.writeFileSync(path.join(aoDir, 'ao.yaml'), 'custom: config');

    await updateProject(tmpDir);
    const content = fs.readFileSync(path.join(aoDir, 'ao.yaml'), 'utf-8');
    expect(content).toBe('custom: config');
  });

  it('preserves memory', async () => {
    const memDir = path.join(tmpDir, '.ao', 'memory', 'review-patterns');
    fs.mkdirSync(memDir, { recursive: true });
    fs.writeFileSync(path.join(memDir, 'security.md'), 'learned pattern');

    await updateProject(tmpDir);
    const content = fs.readFileSync(path.join(memDir, 'security.md'), 'utf-8');
    expect(content).toBe('learned pattern');
  });
});
```

- [ ] **Step 2: update 実装**

```typescript
// src/commands/update.ts
import fs from 'fs';
import path from 'path';
import { execSync } from 'child_process';
import { getTemplatePath, getTargetPaths } from '../utils/paths.js';
import { parseTopLevelYaml } from '../utils/yaml.js';
import { log } from '../utils/logger.js';

function hasCustomizations(srcDir: string, destDir: string): string[] {
  const modified: string[] = [];
  if (!fs.existsSync(srcDir) || !fs.existsSync(destDir)) return modified;
  for (const file of fs.readdirSync(srcDir)) {
    const srcPath = path.join(srcDir, file);
    const destPath = path.join(destDir, file);
    if (fs.statSync(srcPath).isFile() && fs.existsSync(destPath)) {
      const srcContent = fs.readFileSync(srcPath, 'utf-8');
      const destContent = fs.readFileSync(destPath, 'utf-8');
      if (srcContent !== destContent) modified.push(file);
    }
  }
  return modified;
}

function copyDir(srcDir: string, destDir: string) {
  if (!fs.existsSync(srcDir)) return;
  fs.mkdirSync(destDir, { recursive: true });
  for (const file of fs.readdirSync(srcDir)) {
    const srcPath = path.join(srcDir, file);
    const destPath = path.join(destDir, file);
    if (fs.statSync(srcPath).isDirectory()) {
      copyDir(srcPath, destPath);
    } else {
      fs.copyFileSync(srcPath, destPath);
    }
  }
}

export async function updateProject(projectRoot: string) {
  log.header('konbini update');

  const paths = getTargetPaths(projectRoot);

  // schema_version チェック
  const aoConfigPath = paths.aoConfig;
  if (fs.existsSync(aoConfigPath)) {
    const content = fs.readFileSync(aoConfigPath, 'utf-8');
    const config = parseTopLevelYaml(content);
    if (config.schema_version && config.schema_version < 1) {
      log.warn('ao.yaml の schema_version が古いです。マイグレーションが必要かもしれません。');
    }
  }

  // カスタマイズ検出（agents）
  const customized = hasCustomizations(getTemplatePath('agents'), paths.agents);
  if (customized.length > 0) {
    log.warn(`以下のファイルがカスタマイズされています: ${customized.join(', ')}`);
    log.info('上書きすると変更が失われます。バックアップを推奨します。');
    // 非インタラクティブ環境（CI等）では警告のみで続行
  }

  // テンプレートファイルのみ上書き
  copyDir(getTemplatePath('agents'), paths.agents);
  log.success('agents updated');

  copyDir(getTemplatePath('commands'), paths.commands);
  log.success('commands updated');

  copyDir(getTemplatePath('rules'), paths.rules);
  log.success('rules updated');

  copyDir(getTemplatePath('spec-templates'), paths.specTemplates);
  log.success('spec templates updated');

  log.info('ao.yaml, memory, steering は保持しました');
  log.success('update complete');
}

export async function runUpdate(_args: string[]) {
  await updateProject(process.cwd());
}
```

- [ ] **Step 3: テスト実行 → Commit**

```bash
npx vitest run test/commands/update.test.ts
git add src/commands/update.ts test/commands/update.test.ts
git commit -m "feat: update command (preserves config and memory)"
```

---

### Task 15: config show + memory simplify コマンド (スタブ)

**Files:**
- Create: `src/commands/config-show.ts`
- Create: `src/commands/memory-simplify.ts`

- [ ] **Step 1: config-show 実装**

```typescript
// src/commands/config-show.ts
import fs from 'fs';
import path from 'path';
import { log } from '../utils/logger.js';

export async function runConfigShow(_args: string[]) {
  const configPath = path.join(process.cwd(), '.ao', 'ao.yaml');
  if (!fs.existsSync(configPath)) {
    log.error('ao.yaml が見つかりません。npx konbini init を実行してください。');
    process.exit(1);
  }
  log.header('konbini config');
  console.log(fs.readFileSync(configPath, 'utf-8'));
}
```

- [ ] **Step 2: memory-simplify 実装（v0.1: Claude Code セッション経由のみ。CLI実行はガイダンス表示）**

```typescript
// src/commands/memory-simplify.ts
import { log } from '../utils/logger.js';

export async function runMemorySimplify(_args: string[]) {
  log.header('konbini memory simplify');
  log.info('Memory simplify は Claude Code セッション内で実行されます。');
  log.info('');
  log.info('Claude Code で以下を実行してください:');
  log.info('  「.ao/memory/ の review-patterns を整理・統合してください」');
  log.info('');
  log.info('orchestrator が自動で simplify_threshold (20) に達した際にも実行されます。');
}
```

- [ ] **Step 3: Commit**

```bash
git add src/commands/
git commit -m "feat: config show and memory simplify commands"
```

---

### Task 16: README.md

**Files:**
- Create: `README.md`

- [ ] **Step 1: README 作成**

以下を含む:
- プロジェクト概要（cc-sdd inspired を明記）
- クイックスタート（`npx konbini init`）
- Prerequisites（ツール + プラグイン一覧）
- ワークフロー図（上流→下流）
- ao.yaml の設定例（3プリセット）
- レビューブレイクポイント一覧
- 学習ループの説明
- ライセンス（MIT）
- クレジット（cc-sdd へのリンク）

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: README with quickstart, workflow, and configuration"
```

---

### Task 17: ビルド + ローカルテスト + npm publish 準備

**Files:**
- Modify: `package.json` (repository URL 確定)

- [ ] **Step 1: 全テスト実行**

```bash
npm test
```

Expected: ALL PASS

- [ ] **Step 2: ビルド**

```bash
npm run build
```

Expected: `dist/` にコンパイル済みファイル

- [ ] **Step 3: ローカルテスト**

```bash
cd /tmp && mkdir test-project && cd test-project && git init
npx /path/to/konbini init solo
ls -la .ao/ .claude/ .kiro/
```

Expected: 全ディレクトリが生成される

- [ ] **Step 4: npm publish dry-run**

```bash
npm pack --dry-run
```

Expected: dist/ と templates/ が含まれる

- [ ] **Step 5: Commit + Tag**

```bash
git add -A
git commit -m "chore: prepare v0.1.0 release"
git tag v0.1.0
```

---

## Summary

| Chunk | タスク数 | 概要 |
|-------|---------|------|
| 1 | 3 | プロジェクト基盤 + CLI + ユーティリティ |
| 2 | 3 | チェッカー + テンプレートコピー + init |
| 3 | 4 | ao.yaml + 3 agent テンプレート（核心） |
| 4 | 4 | SDD コマンド + ルール + spec テンプレート + スモークテスト |
| 5 | 4 | update/config/memory コマンド + README + publish |

合計 18 タスク。Chunk 3（agent テンプレート）が最も重要で工数がかかる。特に orchestrator.md は 500行超のプロンプトエンジニアリング。

### v0.1 で意図的にスコープ外としたもの

- `npx konbini memory simplify` の CLI 実行（Claude Code セッション内で実行を案内）
- プラグインの自動検出（ユーザーに確認を案内）
- `update` コマンドのインタラクティブ diff 確認（警告のみ。v0.2 で対話型にする）
